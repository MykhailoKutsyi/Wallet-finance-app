import styled from "styled-components";

export const DatePickWrapper = styled.div`
    margin: 0px;
    padding: 0px;
`;

export const DatePickHeader = styled.h4`
    padding: 20px 0px;
    text-align: center;
`;

export const DatePickSubheader = styled.p`
    display: flex;
    justify-content: space-evenly;
    width: 100%;
`;

export const SubHeaderSpan = styled.span`
    width: 50%;
    text-align: center;
`;

export const Input = styled.input`
    box-sizing: border-box;
    height: 50px;
    width: 45%;
    padding: 13px 16px;
    outline: none;
    border: 1px solid #DCDCDF;
    border-radius: 50px;
`;

export const Form = styled.form`
    padding: 0px 20px;
    display: flex;
    justify-content: space-between;
`;